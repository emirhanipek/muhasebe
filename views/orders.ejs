<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f4f4f4;
        }
        .main-header {
            background-color: #2c3e50;
            color: white;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .main-header nav ul {
            display: flex;
            list-style: none;
        }
        .main-header nav ul li {
            margin-left: 20px;
        }
        .main-header nav ul li a {
            color: white;
            text-decoration: none;
        }
        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .user-info button {
            background-color: #e74c3c;
            border: none;
            color: white;
            padding: 5px 10px;
            border-radius: 3px;
            cursor: pointer;
        }
        .page-container {
            display: flex;
            flex: 1;
        }
        .sidebar {
            width: 250px;
            background-color: #34495e;
            color: white;
            padding: 20px;
            display: flex;
            flex-direction: column;
            min-height: calc(100vh - 60px - 60px); /* 100vh - header - footer */
        }
        .sidebar-header {
            padding-bottom: 15px;
            border-bottom: 1px solid #4a6278;
        }
        .sidebar-menu {
            flex: 1;
            list-style: none;
            padding-top: 15px;
        }
        .sidebar-menu li {
            margin-bottom: 10px;
        }
        .sidebar-menu li a {
            color: white;
            text-decoration: none;
            display: block;
            padding: 8px 0;
            border-radius: 4px;
        }
        .sidebar-menu li a:hover {
            background-color: #2c3e50;
            padding-left: 5px;
        }
        .sidebar-footer {
            border-top: 1px solid #4a6278;
            padding-top: 15px;
            font-size: 0.8rem;
            color: #bbb;
        }
        .main-content {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 1000px;
            margin: 0 auto;
        }
        h1 {
            color: #333;
            border-bottom: 1px solid #eee;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .order-list {
            list-style: none;
        }
        .order-item {
            padding: 15px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
        }
        .order-item:hover {
            background-color: #f9f9f9;
        }
        .order-id {
            font-weight: bold;
            color: #2c3e50;
        }
        .order-date {
            color: #7f8c8d;
        }
        .order-amount {
            font-weight: bold;
            color: #27ae60;
        }
        .status {
            display: inline-block;
            padding: 3px 8px;
            border-radius: 3px;
            font-size: 0.8em;
        }
        .status-pending {
            background-color: #f39c12;
            color: white;
        }
        .status-delivered {
            background-color: #27ae60;
            color: white;
        }
        .status-cancelled {
            background-color: #e74c3c;
            color: white;
        }
        .main-footer {
            background-color: #2c3e50;
            color: white;
            padding: 20px;
            display: flex;
            justify-content: space-between;
        }
        .footer-links a {
            color: #bbb;
            margin-left: 15px;
            text-decoration: none;
        }
        .footer-links a:hover {
            color: white;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="page-container">
        <%- include('partials/sidebar') %>
        
        <div class="main-content">
            <div class="container">
                <h1><%= title %></h1>
                
                <ul class="order-list">
                    <% siparisler.forEach(siparis => { %>
                        <li class="order-item">
                            <div>
                                <span class="order-id">Sipariş #<%= siparis.id %></span>
                                <p>Müşteri: <%= siparis.musteri %></p>
                                <span class="order-date"><%= siparis.tarih %></span>
                            </div>
                            <div>
                                <span class="order-amount">₺<%= siparis.tutar %></span>
                                <p>
                                    <span class="status status-<%= siparis.durum %>">
                                        <%= siparis.durumMetni %>
                                    </span>
                                </p>
                            </div>
                        </li>
                    <% }) %>
                </ul>
            </div>
        </div>
    </div>
    
    <%- include('partials/footer') %>

    <script>
        console.log('Orders page loaded');
    </script>
</body>
</html>